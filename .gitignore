node_modules
dist
.DS_Store
*.local
.env
.env.*
.playwright-mcp
*.png
*.jpg
*.jpeg
*.pdf
.idea/
.mcp.json

# ...but static assets in public/ must ship with the build (override the
# blanket *.png/*.jpg/*.pdf ignores above) — photo, CV, org logos.
!public/**

# local AI project instructions — not for the public repo
CLAUDE.md
